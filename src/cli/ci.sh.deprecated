#!/usr/bin/env bash
# ci.sh - DEPRECATED: Use 'nself dev ci' instead

printf "\033[0;33m⚠\033[0m  The 'nself ci' command is deprecated.\n"
printf "   Please use: \033[1mnself dev ci\033[0m\n\n"

# Delegate to new command
exec "$(dirname "${BASH_SOURCE[0]}")/dev.sh" ci "$@"
