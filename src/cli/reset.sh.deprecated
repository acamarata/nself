#!/usr/bin/env bash
# reset.sh - DEPRECATED: Use 'nself backup reset' instead

printf "\033[0;33m⚠\033[0m  The 'nself reset' command is deprecated.\n"
printf "   Please use: \033[1mnself backup reset\033[0m\n\n"

# Delegate to new command
exec "$(dirname "${BASH_SOURCE[0]}")/backup.sh" reset "$@"
