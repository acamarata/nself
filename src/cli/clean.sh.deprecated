#!/usr/bin/env bash
# clean.sh - DEPRECATED: Use 'nself backup clean' instead

printf "\033[0;33m⚠\033[0m  The 'nself clean' command is deprecated.\n"
printf "   Please use: \033[1mnself backup clean\033[0m\n\n"

# Delegate to new command
exec "$(dirname "${BASH_SOURCE[0]}")/backup.sh" clean "$@"
