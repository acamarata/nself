#!/usr/bin/env bash
# rollback.sh - DEPRECATED: Use 'nself backup rollback' instead

printf "\033[0;33m⚠\033[0m  The 'nself rollback' command is deprecated.\n"
printf "   Please use: \033[1mnself backup rollback\033[0m\n\n"

# Delegate to new command
exec "$(dirname "${BASH_SOURCE[0]}")/backup.sh" rollback "$@"
