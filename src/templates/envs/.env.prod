# ╔═══════════════════════════════════════════════════════╗
# ║            PRODUCTION CONFIGURATION                   ║
# ╠═══════════════════════════════════════════════════════╣
# ║ Non-sensitive production settings only                ║
# ║ Secrets go in .env.secrets (not committed)            ║
# ╚═══════════════════════════════════════════════════════╝

# Production overrides (non-sensitive only)
# Run 'nself prod' to generate secure passwords

# Core Settings
# PROJECT_NAME=myproject
# BASE_DOMAIN=api.example.com

# Production URLs (public endpoints)
# HASURA_ROUTE=api.${BASE_DOMAIN}
# AUTH_ROUTE=auth.${BASE_DOMAIN}
# STORAGE_ROUTE=storage.${BASE_DOMAIN}
# AUTH_CLIENT_URL=https://app.example.com
# AUTH_SERVER_URL=https://auth.example.com

# External Integrations
# HASURA_GRAPHQL_REMOTE_SCHEMAS=https://remote.example.com/graphql
# HASURA_GRAPHQL_ACTIONS_HANDLER_WEBHOOK_BASEURL=https://webhooks.example.com
# HASURA_GRAPHQL_WEBHOOK_HANDLER_WEBHOOK_BASEURL=https://events.example.com

# Production Features
# HASURA_GRAPHQL_ENABLE_CONSOLE=false
# HASURA_GRAPHQL_DEV_MODE=false
# HASURA_GRAPHQL_ENABLE_TELEMETRY=false
# HASURA_GRAPHQL_UNAUTHORIZED_ROLE=public

# Production CORS: Strict - Only your domain (NO wildcards, NO localhost)
HASURA_GRAPHQL_CORS_DOMAIN=https://*.${BASE_DOMAIN}

# SECURITY: PostgreSQL Port Exposure
# Production: Never expose database to host (internal Docker network only)
POSTGRES_EXPOSE_PORT=false

# SSL_MODE=letsencrypt

# Resource Configuration
# POSTGRES_MAX_CONNECTIONS=200
# POSTGRES_SHARED_BUFFERS=256MB
# POSTGRES_EFFECTIVE_CACHE_SIZE=1GB
# HASURA_GRAPHQL_PG_CONNECTIONS=50
# REDIS_MAXMEMORY=1gb
# REDIS_MAXMEMORY_POLICY=allkeys-lru

# Production Services
# REDIS_ENABLED=true
# PROMETHEUS_ENABLED=true
# GRAFANA_ENABLED=false
# LOKI_ENABLED=true

# Email Configuration
# EMAIL_PROVIDER=ses
# EMAIL_FROM=noreply@example.com
# AWS_REGION=us-east-1

# Monitoring & Logging
# LOG_LEVEL=warn
# HASURA_GRAPHQL_ENABLED_LOG_TYPES=startup
# SENTRY_ENABLED=true
# SENTRY_ENVIRONMENT=production