# nself dr

**This command moved to a plugin.**

`nself dr` is no longer part of the CLI core.

## Install

```bash
nself install dr
```

Once installed, `nself dr ...` works exactly as before — the CLI proxies the
command to the plugin, and passes it the project settings its manifest declares.

---

← [[Commands]] · [[Plugin-Overview]] · [[Home]]
