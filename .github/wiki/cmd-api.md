# nself api

**This command moved to a plugin.**

`nself api` is no longer part of the CLI core.

## Install

```bash
nself install api
```

Once installed, `nself api ...` works exactly as before — the CLI proxies the
command to the plugin, and passes it the project settings its manifest declares.

---

← [[Commands]] · [[Plugin-Overview]] · [[Home]]
