# nself queue

**This command moved to a plugin.**

`nself queue` is no longer part of the CLI core.

## Install

```bash
nself install queue
```

Once installed, `nself queue ...` works exactly as before — the CLI proxies the
command to the plugin, and passes it the project settings its manifest declares.

---

← [[Commands]] · [[Plugin-Overview]] · [[Home]]
