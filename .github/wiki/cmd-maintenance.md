# nself maintenance

**This command moved to a plugin.**

## Install

```bash
nself install maintenance
```

Once installed, `nself maintenance ...` works exactly as before — the CLI proxies the
command to the plugin.

---

← [[Commands]] · [[Plugin-Overview]] · [[Home]]
