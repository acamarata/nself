# nself flags

**This command moved to a plugin.**

## Install

```bash
nself install flags
```

Once installed, `nself flags ...` works exactly as before — the CLI proxies the
command to the plugin.

---

← [[Commands]] · [[Plugin-Overview]] · [[Home]]
