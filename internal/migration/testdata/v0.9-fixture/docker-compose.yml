# Generated by nself
# v0.9.9 — DO NOT EDIT (this header triggers v0.9 detection in v1 CLI)
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: nself
      POSTGRES_PASSWORD: devsecret
      POSTGRES_DB: nself_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  hasura:
    image: hasura/graphql-engine:v2.35.0
    depends_on:
      - postgres
    environment:
      HASURA_GRAPHQL_DATABASE_URL: postgres://nself:devsecret@postgres:5432/nself_db
      HASURA_GRAPHQL_ENABLE_CONSOLE: "true"
      HASURA_GRAPHQL_ADMIN_SECRET: devsecret
    ports:
      - "8080:8080"

  nginx:
    image: nginx:1.25-alpine
    volumes:
      - ./nginx:/etc/nginx/conf.d
    ports:
      - "80:80"
      - "443:443"

volumes:
  postgres_data:
