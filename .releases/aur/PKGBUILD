# Maintainer: Aric Camarata <aric.camarata@gmail.com>
pkgname=nself
pkgver=0.4.5
pkgrel=1
pkgdesc="Production-ready self-hosted backend infrastructure"
arch=('any')
url="https://nself.org"
license=('custom:Source-Available')
depends=('bash' 'docker' 'docker-compose')
source=("$pkgname-$pkgver.tar.gz::https://github.com/acamarata/nself/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('bfd6022c4d9118c4522662524104ba9db9de2f382bca32d1923a06718f943441')

package() {
    cd "$srcdir/$pkgname-$pkgver"
    
    # Create directories
    install -dm755 "$pkgdir/usr/share/$pkgname"
    install -dm755 "$pkgdir/usr/bin"
    
    # Copy source files
    cp -r src "$pkgdir/usr/share/$pkgname/"
    cp -r bin "$pkgdir/usr/share/$pkgname/"
    
    # Create wrapper script
    cat > "$pkgdir/usr/bin/$pkgname" << 'EOF'
#!/bin/bash
exec /usr/share/nself/bin/nself "$@"
EOF
    chmod 755 "$pkgdir/usr/bin/$pkgname"
    
    # Set permissions
    find "$pkgdir/usr/share/$pkgname" -type f -name "*.sh" -exec chmod 755 {} \;
    
    # Install documentation
    install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
    install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}